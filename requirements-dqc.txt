# Runtime dependencies of the app — the API, its four screens and the
# agent loop behind them. requirements.txt adds only the test runner.

# API + static frontend
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9

# LLM transport (OpenAI-compatible gateways / Ollama / LiteRT + Bedrock)
httpx>=0.27
pyyaml>=6.0
boto3>=1.34

# Excel parsing — dictionaries and data tables uploaded as .xlsx
# (.csv needs nothing beyond the standard library)
openpyxl>=3.1
