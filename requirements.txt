# DQC PoC — local full-stack development dependency set.
#
# Superset of requirements-dqc.txt (which the slim production image uses).
# The DQC generator only needs the non-heavies below — no torch/kuzu/chromadb/
# scikit-learn/umap (those back the SAS field-diff explainer and knowledge-graph
# builder, which are NOT part of this PoC).
#
# Install:  pip install -r requirements.txt

# Core API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9

# Regulation knowledge graph + change-log GraphRAG (pure Python)
networkx>=3.3

# LLM transport (Ollama / LiteRT-LM OpenAI-compatible clients + Bedrock)
httpx>=0.27
pyyaml>=6.0
boto3>=1.34
mangum>=0.17

# Excel parsing — the DQC router reads the field-dictionary workbook and the
# extracted-cases workbook
openpyxl>=3.1

# Tests
pytest>=8.0
